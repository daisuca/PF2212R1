let numbers = [1, 2, 3, 4, 5];
// Vòng lặp for-in
for (let item in numbers) {
    document.write(numbers[item] + "<br>");
}
// câu lệnh for để duyệt mảng
for (let i = 0; i < numbers.length; i++) {
    document.write(numbers[item] + "<br>");
}